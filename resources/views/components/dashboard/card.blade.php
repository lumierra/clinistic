<div class="col-xl-3 col-md-6 col-6">
    <div class="box">
        <div class="box-body">
            <div class="text-center">
                <h1 class="fs-50 text-{{ $color }}">
                    <i class="{{ $icon }}"></i>
                </h1>
                <h2>{{ $value }}</h2>
                <span class="badge badge-pill badge-{{ $color }} px-10 mb-10">{{ $title }}</span>
            </div>
        </div>
    </div>
</div>
